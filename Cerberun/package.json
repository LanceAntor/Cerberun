{
  "name": "cerberun",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "node build-config.js && vite build",
    "preview": "vite preview",
    "build-config": "node build-config.js"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "vite": "^7.0.4"
  }
}
